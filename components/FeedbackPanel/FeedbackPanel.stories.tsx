import React from 'react'

import FeedbackPanel from '.'

export default {
  title: 'FeedbackPanel',
  component: FeedbackPanel,
}

const Template = () => <FeedbackPanel />

export const Default = Template.bind({})
