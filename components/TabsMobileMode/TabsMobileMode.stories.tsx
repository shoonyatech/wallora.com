import React from 'react'

import TabsMobileMode from '.'

export default {
  title: 'TabsMobileMode',
  component: TabsMobileMode,
}

const Template = () => <TabsMobileMode />

export const Default = Template.bind({})
